{% extends "layout.html.twig" %}

{% block title 'admin' %}
{% block content %}
	<form method="post">
		
	</form>
    <h2>Members</h2>
	<table>
			<tr>
				<th>username</th><th>mail</th><th>last login</th><th>last pw change</th>
			</tr>
		{% for user in users %}
			<tr>
				<td>{{ user.username|e }}</td><td>{{ user.mail|e }}</td><td>{{ user.lastlogin|e }}</td><td>{{ user.pwmodified|e }}</td>
			</tr>
		{% endfor %}
	</table>
{% endblock %}
